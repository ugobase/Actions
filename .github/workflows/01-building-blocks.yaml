name: 01 - Building Blocks
on:
    push:
    workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
        - name: Say hello
          run: echo "Hello world"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
        - name: Questionable
          run: |
            echo "Questionable"
            exit 0